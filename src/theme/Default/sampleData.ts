/**
 * Include this file in your project to use the sample data for the Default theme,
 * with strong typing and structure for the data.
 *
 * The original data can be copy/pasted directly from Postman's getResume response
 * into the ./sampleData.json file first.
 */

import { Response } from "@/types/index";
import sampleData from "./sampleData.json";

export const themeDefaultSampleData: Response = sampleData;
